"""Internal-support modules: providers catalog + LiteLLM proxy config.

Public-ish API is re-exported here for convenience but the canonical
import path is `llmcli.support.providers` / `llmcli.support.litellm_config`.
"""
